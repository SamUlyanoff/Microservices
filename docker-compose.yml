version: '3.8'

services:
  #TODO: разобраться с KAFKA

  postgres:
    image: postgres:16-alpine
    container_name: postgres-db
    environment:
      POSTGRES_DB: microservicesDB
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data #том для сохранения данных контейнером
    networks:
      - microservices-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 3s
      retries: 5

  mongodb:
    image: mongo:7.0
    container_name: mongo-db
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: microservicesDB
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - microservices-network
    healthcheck:
      test: ["CMD-SHELL", "mongosh --quiet -u admin -p password --authenticationDatabase admin --eval 'db.runCommand({ping:1})'"]
      interval: 5s
      timeout: 3s
      retries: 5

  api-gateway:
    build:
      context: .
      dockerfile: ./api-gateway/Dockerfile
    container_name: api-gateway
    ports:
      - "8090:8090"
    networks:
      - microservices-network

  auth-service:
    build:
      context: .
      dockerfile: ./auth-service/Dockerfile
    container_name: auth-service
    ports:
      - "8091:8091"
    networks:
      - microservices-network
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      POSTGRES_HOST: postgres-db

  inventory-service:
    build:
      context: .
      dockerfile: ./inventory-service/Dockerfile
    container_name: inventory-service
    ports:
      - "8092:8092"
    networks:
      - microservices-network
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      POSTGRES_HOST: postgres-db

  notification-service:
    build:
      context: .
      dockerfile: ./notification-service/Dockerfile
    container_name: notification-service
    ports:
      - "8093:8093"
    networks:
      - microservices-network
    depends_on:
      mongodb:
        condition: service_healthy
    environment:
      MONGODB_HOST: mongo-db

  order-service:
    build:
      context: .
      dockerfile: ./order-service/Dockerfile
    container_name: order-service
    ports:
      - "8094:8094"
    networks:
      - microservices-network
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      POSTGRES_HOST: postgres-db

  product-service:
    build:
      context: .
      dockerfile: ./product-service/Dockerfile
    container_name: product-service
    ports:
      - "8095:8095"
    networks:
      - microservices-network
    depends_on:
      mongodb:
        condition: service_healthy
    environment:
      MONGODB_HOST: mongo-db

  user-service:
    build:
      context: .
      dockerfile: ./user-service/Dockerfile
    container_name: user-service
    ports:
      - "8096:8096"
    networks:
      - microservices-network
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      POSTGRES_HOST: postgres-db

networks:
  microservices-network:
    driver: bridge

volumes:
  postgres-data:
  mongo-data: